-- ==================================================================
-- EDGE JOURNAL — декілька скрінів сетапу
-- Виконати один раз у Supabase → SQL Editor → New query → Run.
-- Скрипт ідемпотентний: можна запускати повторно без наслідків.
--
-- trade_image (одна картинка) лишається для сумісності зі старими
-- записами й береться як перший елемент масиву при збереженні.
-- trade_images — повний список: галерея скрінів сетапу в модалці
-- запису угоди тепер підтримує декілька зображень, як mistake_images.
-- ==================================================================

alter table public.trades
  add column if not exists trade_images jsonb;
