-- ==================================================================
-- EDGE JOURNAL — архівування акаунтів
-- Виконати у Supabase → SQL Editor. Ідемпотентний.
--
-- Архів — це не «закрито» (closed_at/closed_reason з попередньої
-- міграції): закриття означає «пропа більше нема, але акаунт видно
-- в загальному списку». Архів означає «прибрати з очей назавжди».
-- Тому дія одностороння: анархівувати нема кнопки навмисно.
-- ==================================================================

alter table public.prop_accounts
  add column if not exists archived_at timestamptz;
